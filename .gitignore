node_modules
.env
dist
.DS_Store

# Discovery capture + generated sample artifacts (regenerated at runtime, not source)
generated/
output/

# Local AI-assistant dev tooling (kept locally, not published)
CLAUDE.md
.mcp.json
.claude/
.github/copilot-instructions.md

# Local-only working files (drafts, marketing assets, guides) — kept out of the repo.
# The committed README banner is frontend/src/assets/cover.png (not in docs/).
docs/*

# Raw demo recording source (large; only the optimized demo.gif is committed)
*.mov